<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

class SlutUserTest extends TestCase
{
    /**
     * A basic unit test set status to absent when the user entered without exit.
     */
    public function test_absent_enter_without_exit(): void
    {
        
        $userSluts = collect([

        ]);
        $this->assertTrue(true);
    }
}
